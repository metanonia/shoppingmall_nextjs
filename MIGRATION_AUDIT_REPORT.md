# shoppingmall_php → Next.js 마이그레이션 감사 보고서

- 감사일: 2026-08-08~09 (Asia/Seoul)
- 원본: `../shoppingmall_php`
- 대상: `shoppingmall_nextjs`
- 프로젝트 목적: 기존 운영 데이터 이전이 아니라, PHP 기능을 기준으로 신규 Next.js 쇼핑몰 구축
- 현재 판정: **기능 기준 통과 — PHP 원본 대비 확인된 기능 gap을 구현·검증했으며, 폭·픽셀 차이는 판정에서 제외**

> 아래 최초 감사의 실패 원인과 증거는 이력 보존을 위해 유지한다. 현재 상태는 바로
> 다음의 “최종 재검증 결과”를 기준으로 판단해야 한다.

## 사용자 확정 판정 기준 (2026-08-09)

- PHP 원본에 존재하는 기능을 기준으로 Next.js의 기능·처리·권한·SQL 동작을 비교한다.
- 기능 차이가 없으면 통과로 판정한다. 주석의 스코프아웃 표기는 PHP 원본에 기능이
  있으면 무시한다.
- 화면 폭·픽셀·스타일 차이와 반응형 개편 전의 레이아웃 차이는 migration 판정에서
  제외한다.
- 결제사는 신규 쇼핑몰 설치 후 선정한다. 결제 어댑터 구현 방법이
  `PAYMENT_ADAPTER_GUIDE.md`에 기록되어 있으면 현재 결제 항목은 완료로 본다.
- 현재 기능상 미통과 항목은 없으며, 실제 운영키·PG·스토리지·운영 절차는 신규 쇼핑몰
  운영 준비 단계의 후속 작업이다.

## 최종 재검증 결과 (2026-08-09)

PHP 원본 파일과 Next.js 대응 코드를 다시 대조하면서 발견된 운영상 오류를 수정했다.
이번 후속 작업은 문서만 고친 것이 아니라 코드, Prisma 모델, 순차 SQL migration,
고객몰/관리자 화면을 함께 변경한 결과다.

| 검증 항목 | 최종 결과 |
|---|---|
| Core 단위 테스트 | 14개 파일, 74개 테스트 통과 |
| Storefront ESLint | 오류 0, 경고 20 |
| Backoffice ESLint | 오류 0, 경고 8 |
| Storefront production build | 성공, 정적 생성 단계 53/53 완료 |
| Backoffice production build | 성공, 정적 생성 단계 85/85 완료 |
| 빈 DB migration | SQL 29개(001~030, 014 없음) 순차 적용 성공 |
| 빈 DB seed | 상품 12개 등 전체 시드 성공 |
| 검증 DB | 52개 테이블, migration 이력 29개 |
| Storefront HTTP | 홈·상품·후기·가입완료 200, 보호 화면 307 |
| Backoffice HTTP | 홈·상품문의·후기 비로그인 307, cron 무인증 401 |
| 공백/patch 검사 | `git diff --check` 통과 |

후속 PHP–Next.js 대조에서 수정한 핵심 항목은 다음과 같다.

- 회원 승인 정책: PHP처럼 승인대기 회원은 가입 직후 로그인·장바구니 병합을 하지
  않고, 완료 화면에서 관리자 승인 후 로그인 가능함을 안내한다.
- 회원탈퇴: `mallRN_member_withdrawal` 감사 이력을 남기고 주문/상태로그 ID 익명화,
  쿠폰·마일리지·검색·후기·문의·찜·장바구니 삭제, 게시글/댓글 작성자 익명화를 하나의
  DB transaction으로 처리한다.
- 구매후기: PHP `mallRN_review`를 이식하고 본인 배송완료·구매확정 주문상품만 1회
  작성하도록 서버에서 검증한다. 상품상세, 전체후기, 내후기, 관리자 베스트/삭제를
  연결하고 PHP의 첨부파일 5개 업로드·표시를 추가했다.
- 상품문의: 기존 회원 작성/조회에 관리자 답변 저장과 삭제 화면을 추가했다.
- 상품문의 추가 패리티: PHP 설정값에 따른 비회원 작성 허용, 비밀글 강제/선택/해제,
  비회원 비밀번호 열람, 작성자명 보호, 문의 분류, 개인정보 동의와 첨부파일 5개를
  구현했다.
- 휴면회원: 로그인 시 `mallRN_member_sleep`의 비밀번호를 확인해 활성 회원으로 자동
  복구하고, 관리자 수동 복구 및 복구 완료 화면을 추가했다.
- 회원가입/수정: 가입 마일리지와 가입축하 쿠폰, 환영 메일/SMS를 연결했다. 생년월일,
  성별, 결혼여부, 직업, 관심분야, 사업자정보, 관리자 정의 추가항목 5개를 설정에 따라
  표시·필수검증·저장하도록 확장했다.
- 자동메일: 커스텀 템플릿 여부와 PHP의 `send` 발송 여부를 분리하고 가입/주문 메일의
  발송 설정을 관리자 화면과 실제 발송 흐름에 연결했다. PHP의 `common` 공통 레이아웃,
  판매사 가입(`vjoin`), 상품별 배송(`delivery`) 메일도 복원했다.
- 문자 알림: PHP `mallRN_sms_auto` 7개 템플릿과 고객/관리자 발송 토글을 이식하고
  가입·인증번호·무통장 주문접수/입금확인·결제완료·판매사 통보·배송 발송 조건을 연결했다.
- 푸시 알림: 관리자/판매사별 PC·모바일 토큰과 수신 여부를 저장하고, FCM HTTP v1로
  신규 회원·주문·문의·게시판 상담 알림을 발송하도록 구현했다.
- 탈퇴회원 운영: 이력 조회·검색·삭제 화면과 회원통계의 기간별 탈퇴자 수를 복원했다.
- 관리자 운영: 본사배송 결제완료·배송준비중 상품의 송장 Excel 다운로드/업로드와
  배송중 일괄전환을 추가했다. 검색어 통계와 방문자·페이지뷰·신규/재방문·PC/모바일·
  브라우저·OS·유입사이트·유입검색어 수집/통계도 고객몰 추적 API와 연결했다.
- 관리자/판매사 대시보드: 주문·매출 외에 미답변 고객/판매사 문의, 상품문의,
  교환·반품·취소, 현금영수증, 승인대기 상품/판매사, 회원, 배송, 오류, 후기, 정산대기
  등 PHP 운영 알림 위젯의 핵심 집계를 복원하고 관리자 위젯 표시·순서 설정에 포함했다.
- 기타 주문정책: 자동 배송완료·자동 구매확정·미결제 주문정리 일수, 스마트택배 사용/API
  키, 주문취소/배송요청 문구를 관리자 설정에 추가하고 일일 batch 및 배송조회 provider가
  실제 설정값을 읽도록 연결했다. 3일 지난 미완성 임시주문 정리도 batch에 포함했다.
- 고객 게시판: 회원 ID/비회원 비밀번호 소유권 검증을 거쳐 게시글 수정·삭제와 댓글
  삭제가 가능하도록 PHP 동작을 이식했다.
- 판매사 게시판: `vnotice`/`vcounsel`을 추가해 판매사는 본사 공지를 읽고 자신의 문의만
  작성·조회하며, 관리자는 판매사 공지 CRUD와 판매사 문의 답변을 처리한다. 고객몰
  라우트에서는 두 게시판 ID를 차단한다.
- 입점사 상품문의: 로그인한 판매사의 `vendor` 값으로 목록과 답변 mutation을 모두
  제한해 자기 상품 문의만 조회·답변하도록 구현했다.
- 현금영수증/R·V 결제: 주문 시 신청정보 입력·필수 설정, 결제확정 후
  `mallRN_order_cash_receipts` 발급요청 생성, 관리자 상태처리 화면을 추가했다.
  실시간계좌이체·가상계좌 타입과 Mock 회귀 경로도 연결했다. 이 프로젝트는 신규 설치 후
  결제사를 선정하므로 특정 PG 승인·취소 및 현금영수증 API 구현은 현재 인수 범위가 아니다.
  선정 후의 계약 확장, callback/return, 전액·부분취소, 현금영수증과 테스트 절차를
  `PAYMENT_ADAPTER_GUIDE.md`에 기록했으므로 사용자 기준상 결제 항목은 완료로 판정한다.
- 쿠폰 자동발급: 가입(type=1), 생일(type=3, 양력/음력·연 1회), 상품상세 다운로드
  (type=4)를 PHP 호출 지점과 맞췄다. 관리자/등급 쿠폰의 반복 발급도 원본처럼 허용한다.
- 최근 본 상품/조회수: 방문자별 최대 30개, 재조회 최신화, 동일 방문자·상품의 하루
  1회 조회수 증가, 로그인 시 비회원 기록 병합, 회원/비회원 공용 플로팅 최근상품
  drawer를 PHP 규칙에 맞게 구현했다.
- 상거래: 구매수량 제한을 회원의 과거 주문+현재 장바구니 기준으로 계산하고,
  무통장입금 계좌/입금자 저장·표시, 상품 쿠폰 `use_limit2`의 회원별 다운로드 횟수
  의미를 원본 SQL대로 수정했다.
- 상품 목록: PHP의 `goods_soldout` 3단계 정책(일반 노출/품절상품 후순위/숨김)과
  동일한 품절 판정식을 카테고리·검색·판매사 목록 및 카테고리 집계에 연결했다.
  결과 내 검색은 상품명·검색용상품명·상품코드·키워드에 대한 다중 검색어 AND 조건으로
  복원했고, 베스트 상품은 최근 7일 주문상품 수 우선+기존 인기순 보충 방식으로 수정했다.
- 상품 운영설정: 옵션명·브랜드·제조사·원산지·상품정보제공고시·아이콘 마스터값을
  관리자 설정과 상품 등록/옵션 빌더에 연결하고, 판매사 옵션명 마스터도 복원했다.
- 교환·반품·취소: PHP `mallRN_order_status_change` 테이블과 상태값을 이식하고,
  회원의 상품별 접수·철회, 비회원 주문 비밀번호 기반 접수·철회, 관리자/판매사 승인·
  회수·교환 재발송·거절 화면, 기존 부분환불 완료와 접수 상태 동기화를 구현했다.
  교환 재발송 시 배송 자동메일/SMS도 교환 표시와 함께 발송한다.
- 입점사 운영: 판매사별 배송비 정책을 장바구니 배송그룹 계산에 연결하고,
  입점 신청 시 사업자등록증·통장사본을 비공개 저장소에 보관한다. 정산에는 세금계산서
  발행 여부·정산 상태·처리일을 추가했다.
- 상품/화면: 회원·등급·상품별 마일리지 적립 규칙, 상품정보제공고시 입력·Excel 이관,
  동일 위치 이미지 팝업 슬라이더를 PHP 규칙에 맞게 추가했다.
- 후속 기능 gap 종결: Firebase 웹 설정·Service Worker·PC/mobile 토큰 자동발급과 로그인
  후 토큰 동기화, 배송 송장 `.xls`/`.xlsx` 동시 업로드, 상품 아이콘 업로드·교체·삭제·
  순서관리, 네이버페이 상품/장바구니 구매·찜·상품정보 XML 경로를 추가했다.
- 대시보드/검색/화면 패리티: PHP `managers/widget`와 `vendor/widget`의 오늘 지표,
  주문단계, 최근 주문·회원·문의·후기·공지·정산 위젯을 복원했다. 게시판·회원·주문은
  다중 검색어 AND 검색을 적용하고 상품 조회수 랭킹, 매출 PC/mobile·결제수단 구분,
  모바일 상품상세 하단 구매 drawer를 추가했다.
- 링크/권한: 깨진 주문·고객센터 링크를 실제 App Router 경로로 교체하고, 모든 확인된
  관리자/입점사 mutation Server Action에서 역할을 재검증한다.

사용자 인수 기준상 PHP 원본에 구현된 기능은 기존 문서에서 스코프아웃으로 합의했더라도
모두 구현 대상이다. 다만 특정 결제사 연동은 신규 쇼핑몰 설치 후 선정·구현하기로 별도
확정했으며, 어댑터 작업 방법이 문서화되면 현재 결제 항목은 완료로 본다. 확인된 비결제
소스 gap은 후속 구현으로 닫았고 아래 검증·배포 조건만 남는다.

- PHP와 Next.js의 화면 폭·픽셀 동일성은 판정 대상에서 제외한다. 화면은 기능 접근과
  동작이 PHP 원본과 동일한지만 확인하며, 차이가 없으면 통과로 판정한다.
- 실 SMTP/SMS/소셜 로그인/FCM 등 외부 서비스는 운영 키와 계약 환경에서 별도
  검증해야 한다.
- FCM과 네이버페이는 코드 경로와 설정 화면까지 완료했으나 실제 서비스계정·계약키를
  사용한 외부 응답 검증은 신규 쇼핑몰의 운영 설정 단계에서 수행한다.
- 특정 PG 승인·취소·부분취소와 현금영수증 자동 발급·취소는 결제사 선정 후
  `PAYMENT_ADAPTER_GUIDE.md`의 운영 전 체크리스트로 관리하며 현재 결함 수에는 포함하지 않는다.
- 백오피스가 storefront public 디렉터리에 파일을 쓰는 구조와 빌드 시 동적 파일 추적
  경고는 object storage 전환 전까지 운영 배포 위험으로 남는다.
- 운영 데이터 이전은 프로젝트 범위가 아니다. SQL 검증 기준은 빈 DB에서 전체 migration과
  seed를 적용해 신규 쇼핑몰을 재현할 수 있는지 여부다.

최신 production HTTP smoke test에서는 storefront 홈·베스트·신상품·다중검색·상품상세·
판매사몰·후기·비회원 주문조회·입점신청이 200을 반환했다. 회원 전용 최근상품은 307,
관리자/판매사 신규 운영 화면은 비로그인 상태에서 모두 307, 인증 없는 cron은 401로
차단됐다. `/list`는 필수 카테고리 파라미터 없이 호출해 404였고, 네이버 feed는 시드의
채널 설정이 꺼져 있어 의도대로 404였다.

## 후속 조치 결과 (2026-08-08)

이 보고서 작성 후 P0 항목을 순차 보완했다. 최초 판정은 감사 시점 기록으로 아래에
유지하며, 현재 코드 상태는 다음과 같다.

- 스토어프론트와 백오피스 production build 통과
- 두 앱 ESLint 오류 0개(성능/레거시 이미지 관련 warning은 남음)
- 모든 보호된 admin/vendor Server Action에 역할 재검증 추가
- 공용 Excel import helper를 외부 호출 가능한 action module에서 `server-only` 모듈로 이동
- 체크섬/잠금/이력 테이블을 사용하는 DB migration runner 추가
- 빈 임시 DB에서 당시 SQL 15개 적용, 재실행 skip, seed 성공 확인
- 루트 `pnpm verify`와 `pnpm db:setup` 명령 추가

따라서 감사 당시의 세 가지 배포 차단 항목(빌드, action 인증, fresh DB setup)은
해소되었다. 이후 SQL 17~27(탈퇴/리뷰/조회기록/휴면복구/가입메일/메일발송 설정/현금영수증/댓글답글/푸시/자동메일/SMS설정)까지 추가하여 총 26개 migration의
fresh DB 적용과 seed를 다시 검증했다. 다만 실 PG/소셜/SMTP/SMS 검증과 공유 object
storage 전환은 신규 쇼핑몰 운영 구성의 별도 후속 범위로 남아 있다. 화면 폭·픽셀
visual regression은 기능 판정에서 제외한다.

추가 PHP 원본 대조에서 `goods_coupon_down_json.php:22-26`의 `use_limit2`는 캠페인
전체 발급량이 아니라 동일 `c_uid`/`g_uid`/`id` 조합, 즉 회원별 상품 쿠폰 다운로드
횟수 제한임을 확인했다. 기존 Next.js의 무조건 1회 제한을 type=4 쿠폰의 설정 한도에
맞도록 수정했다.

## 1. 최초 감사 결론(수정 전 상태)

Next.js 결과물은 단순 시제품이 아니다. 고객몰, 관리자, 입점사, 주문/결제, 게시판,
통계, 배치 작업까지 상당 부분 구현되어 있고 핵심 계산 로직 테스트 63개도 통과한다.
현재 로컬 DB에도 43개 테이블과 시드 데이터가 정상 적용되어 있다.

그러나 다음 이유로 “마이그레이션이 정상 완료되었다”고 판정할 수 없다.

1. **스토어프론트와 백오피스의 프로덕션 빌드가 모두 실패한다.**
2. **관리자 Server Action 다수에 함수 단위 인증/권한 검사가 없다.** 보호 레이아웃은
   화면 접근을 막을 뿐, 변경 API 자체의 권한 검사를 대체하지 못한다.
3. **신규 DB 자동 설치 경로가 없다.** SQL 파일은 있지만 compose 초기화나 migration
   명령에 연결되지 않았고 seed는 테이블이 이미 있다고 가정한다.
4. 원본 기능 중 리뷰, 상품문의 답변, 최근 본 상품, 조회수, 일부 결제수단, 현금영수증,
   판매사 게시판, 방문자 통계 등은 명시적으로 미구현 또는 축소되어 있다.
5. 레거시 CSS 두 파일은 바이트 단위로 동일하지만, 실제 마크업은 React로 재작성됐고
   스킨 자산 196개 중 18개만 남아 있다. 이번 환경에서는 브라우저 시각 자동화가
   연결되지 않아 **픽셀 동일성은 입증되지 않았다**.
6. `MIGRATION.md`에는 현재 코드와 맞지 않는 설명이 여러 곳 남아 있어 인수인계 문서의
   신뢰성이 낮다.

따라서 현재 적절한 상태 표기는 **“광범위한 기능 포팅 완료, 보안·빌드·설치·패리티
보완 필요”**이다.

## 2. 조사 범위와 방법

전체 파일 트리를 인벤토리화하고, 생성물/외부 라이브러리와 자체 구현을 구분했다.

| 항목 | 결과 |
|---|---:|
| PHP 저장소 전체 파일 | 1,579 |
| Next.js 저장소 파일(의존성·`.git`·`.next` 제외) | 500 |
| 고객몰 페이지 / Route Handler | 40 / 8 |
| 백오피스 페이지 / Route Handler | 74 / 9 |
| Core 구현 모듈 / 테스트 파일 | 53 / 11 |
| 원본 스킨 파일 / 이식 스킨 파일 | 196 / 18 |
| 현재 로컬 DB 테이블 | 43 |

검토 방법은 다음과 같다.

- PHP의 `php/`, `board/`, `managers/`, `vendor/`, `install/`, 핵심 `lib/` 파일 목록과
  Next.js의 App Router, Server Action, `packages/core`, Prisma/SQL을 기능별로 대응
- 레거시 `CREATE TABLE` 이름과 Prisma `@@map()` 및 `packages/db/sql/*.sql` 비교
- CSS SHA-256 비교, 이미지/스킨 자산 수 비교
- 인증 경계, 업로드, HTML sanitize, 결제 콜백, cron 인증 코드 정적 검사
- Vitest, ESLint, 두 앱의 `next build` 실행
- 실행 중인 로컬 MySQL의 실제 테이블/행 수 확인
- 실행 중인 개발 서버의 대표 공개 URL 및 인증 리다이렉트 HTTP 확인

외부 PHPExcel, PHPMailer, PG사 SDK, 폰트/이미지는 개별 라이브러리 내부 구현을
재검토하기보다 “어떤 기능이 이를 사용했고 Next.js에서 무엇으로 대체했는가”를
확인했다.

## 3. 최초 자동 검증 결과(수정 전 상태)

| 검증 | 결과 | 판정 |
|---|---|---|
| `pnpm --filter @shoppingmall/core test` | 11 파일, 63 테스트 통과 | 통과 |
| 스토어프론트 ESLint | 10 errors, 19 warnings | 실패 |
| 백오피스 ESLint | 4 errors, 7 warnings | 실패 |
| 스토어프론트 production build | TypeScript 오류 | 실패 |
| 백오피스 production build | TypeScript 오류 + 파일 추적 경고 | 실패 |
| MySQL 컨테이너 | healthy, 43개 테이블 | 통과 |
| 고객몰 대표 공개 경로 | 홈/상품/스토어/CS/약관/게시판 등 200 | 부분 통과 |
| 관리자 `/` | 비로그인 시 307 → 로그인 | 통과 |
| cron `/api/cron/daily` | 인증 없이 401 | 통과 |

두 빌드는 공통으로 다음 오류에서 중단된다.

- `apps/*/lib/auth.ts`: 모듈 상단의 환경변수 검사 후에도 `SECRET`이
  `string | undefined`로 추론되어 `signSession` 호출이 실패
- `packages/core/src/categories.ts`: 앱 `tsconfig` target이 ES2017인데 `0n` BigInt
  리터럴 사용

즉 개발 서버가 뜨는 것과 production build 가능 여부가 서로 다르다.

HTTP 점검에서 `/list`는 카테고리 파라미터 없이 404였으며, `/feed/naver`도 설정이
꺼진 시드 상태에서 404였다. 후자는 설정 기반 게이팅으로 볼 수 있으나 모니터링에서는
오류와 구분할 필요가 있다.

## 4. 치명도별 발견 사항

### Critical — 관리자 변경 작업의 인증/권한 검사 누락

다수의 `"use server"` 액션이 `requireAdmin()` 또는 동등한 세션 검사를 호출하지 않은
채 바로 core mutation을 수행한다. 확인된 대표 범위:

- 배너/팝업/CMS 페이지 CRUD
- 상품 생성·수정·승인, 카테고리 CRUD, 기획전 CRUD
- 기본/배송/결제/회원/약관/상품/소셜 설정 변경
- 입점사 승인 변경
- 일부 메일 템플릿 및 회원등급 설정

예를 들어 `banners/actions.ts`의 `createBannerAction()`은 즉시 `createBanner()`를,
`settings/actions.ts`의 각 액션은 즉시 전역 설정 update 함수를 호출한다. 보호
`layout.tsx`는 페이지 렌더링만 보호한다. Server Action 요청은 액션 식별자가 노출되거나
재사용될 수 있으므로 **각 exported mutation 시작점에서 역할을 검증해야 한다**.

권고: 모든 admin 액션 첫 줄에 `await requireAdmin()`, vendor 액션에는
`await requireVendor()`를 강제하고, core mutation에도 소유권/역할 인자를 명시한다.
ESLint 규칙 또는 테스트로 무가드 exported server action을 차단한다.

### High — 두 애플리케이션 모두 배포 빌드 실패

개발 모드의 정상 렌더링만으로 완료 처리되어 production 타입 검증이 깨진 상태다.
`target: ES2020` 이상 적용 또는 BigInt 생성 방식 변경, 환경변수 narrowing 수정 후 두
앱 모두 `next build`를 CI 필수 조건으로 둬야 한다.

### High — 신규 설치/재현 가능한 DB migration 부재

루트 스크립트에는 DB up/down만 있고 SQL 적용 명령이 없다. compose volume에도
`/docker-entrypoint-initdb.d` 마운트가 없다. `seed.js`는 첫 단계부터 기존 테이블을
삭제/삽입하므로 빈 DB에서는 실패한다. 현재 DB가 정상인 것은 SQL을 수동 적용한 기존
볼륨이 있기 때문이다.

권고: 순서가 보장된 migration runner를 추가하고, 새 빈 volume에서
`db:up → migrate → generate → seed → build → smoke`를 CI로 검증한다. SQL 번호에
`014`가 빠진 점도 정리한다.

### High — 운영 배포에서 업로드 구조가 깨질 가능성

백오피스가 `../storefront/public/image`에 직접 파일을 쓴다. 두 앱이 동일한 writable
filesystem을 공유하고 Next.js static public 파일을 런타임에 갱신할 수 있다는 전제다.
서버리스, 다중 인스턴스, read-only image, 독립 컨테이너 배포에서는 동작하지 않는다.
백오피스 빌드도 이 동적 경로 때문에 프로젝트 전체 tracing 경고를 낸다.

권고: S3 호환 object storage 등 공유 저장소와 영속 URL로 교체한다.

### Medium — 1:1 기능 패리티가 아님

원본에는 있으나 미구현/축소된 주요 기능은 다음과 같다.

| 영역 | 상태 |
|---|---|
| 상품 리뷰 | 작성·수정·첨부·관리자 처리와 판매사 소유 후기 조회 화면 완료 |
| 상품문의 | 비회원 작성·비밀번호 열람·첨부·관리자/입점사 답변 완료 |
| 최근 본 상품/조회수 | 방문자별 기록·일 1회 조회수·회원 병합·내 최근상품 화면 완료 |
| 결제 | **완료** — C/H/R/V·Mock 회귀 흐름과 결제사 선정 후 어댑터 구현 가이드 완료 |
| 현금영수증 | **현재 범위 완료** — 신청·요청·관리자 상태처리 및 선정 PG 연결 방법 문서화 |
| 쿠폰 자동발급 | 가입·생일·상품상세 다운로드 및 관리자/등급 발급 규칙 완료 |
| 휴면회원 | 전환·로그인 자동 재활성화·관리자 수동 복구 완료 |
| 판매사 게시판 | `vnotice` 공지와 `vcounsel` 판매사별 문의·관리자 답변 완료 |
| 고객 게시글 | 회원/비회원 게시글 수정·삭제, 댓글 답글 계층·삭제 완료 |
| 관리자 운영 | 방문자/유입/키워드 통계, 배송 송장 `.xls`/`.xlsx`, 탈퇴회원, 전체 계열 대시보드 위젯 완료 |
| 입점사 운영 | 세금계산서/정산상태와 문의·교환반품·배송·후기·정산 및 PHP 계열 대시보드 위젯 완료 |
| 푸시 | FCM HTTP v1, Firebase 설정, Service Worker, PC/mobile 토큰 자동발급·동기화 완료; 운영키 검증만 분리 |
| 쇼핑 연동 | 네이버 상품 feed와 네이버페이 상품/장바구니 구매·찜·상품정보 XML 경로 완료; 계약키 검증만 분리 |
| 검색/목록 UX | 상품·게시판·회원·주문의 다중 AND 검색, 조회수 랭킹, 모바일 구매 drawer 완료 |
| 상품 아이콘 | 파일 업로드·교체·삭제·순서관리와 상품 등록 선택 연결 완료 |
| 게시판 모델 | 동적 게시판별 테이블/권한설정을 공유 테이블+TS 상수로 재설계 |

공유 테이블, TypeScript 설정, 번호형 페이지 이동처럼 데이터와 권한 동작을 유지하는 내부
현대화에 따른 화면 폭·스타일 차이는 기능 gap으로 보지 않는다. PHP 기능이 Next.js에서
동일하게 접근·동작하면 통과로 판정한다.

### Medium — SQL 모델은 의도적으로 통합·재설계됨

레거시에서 검출된 테이블 이름은 약 60여 개, 현재 검증 DB는 52개다. 다음은 단순
누락이 아니라 통합/대체된 사례다.

- `mallRN_admin_log` + `mallRN_vendor_log` → `mallRN_access_log`
- 동적 `mallRN_board_{id}` → `mallRN_board_post` + `mallRN_board_comment`
- `mallRN_mileage_log` → `mallRN_mileage`의 soft-delete 컬럼
- 카테고리 숫자 경로 규칙 → 명시적 parent 관계

후속 작업으로 review, visitor, cash receipt, withdrawal audit, 교환·반품 접수 테이블은
추가됐다. 다만 일부 레거시 통계·목록설정 테이블은 통합 모델 또는 애플리케이션 설정으로
대체됐다. 이 프로젝트는 신규 쇼핑몰 구축이 목적이므로 기존 PHP DB를 변환하는 ETL이나
데이터 rollback 스크립트는 필수 산출물이 아니다. 대신 신규 설치 SQL과 seed의 재현성을
인수 기준으로 사용한다.

### Medium — 테스트 범위가 핵심 계산에 편중

69개 테스트는 가격, 장바구니, 쿠폰, 주문, 결제 어댑터, 메일/SMS, sanitize, 품절 판정 등에
집중되어 있다. core 테스트 파일은 12개이며 다음 자동 검증은 저장소에 없다.

- 커밋된 Playwright/E2E suite와 설정
- 관리자/입점사 권한 거부 테스트
- 실제 MySQL을 사용한 transaction/integration test
- fresh install migration test
- 두 앱 build/lint를 실행하는 루트 CI script
- 실제 PG/SMTP/SMS/소셜 OAuth 계약 테스트

`MIGRATION.md`가 과거 Playwright 수동 검증을 서술하지만 재실행 가능한 테스트 산출물이
없어 회귀 방지 근거로 사용할 수 없다.

### Medium — 문서가 현재 코드와 불일치

`MIGRATION.md`에는 다음과 같은 stale 설명이 공존한다.

- `mallRN_order_sales`를 추가하지 않았다고 하나 현재 Prisma/SQL/DB에 존재
- `mallRN_vendor_configuration`이 없다고 하나 현재 존재
- 팝업 관리자 CRUD가 없다고 하나 현재 백오피스에 존재
- `mallRN_auto_mail`을 쓰지 않는다고 하나 현재 테이블과 관리 화면 존재
- “9단계 완료”와 “리뷰 테이블 없음/상품문의 답변 없음”이 함께 기재

문서의 완료 체크는 최신 코드에서 자동 생성하거나, 최소한 “현재 사실”과 “과거 단계별
결정 기록”을 별도 문서로 분리해야 한다.

### Low/Medium — 린트 및 화면 품질 문제

- 내부 이동에 `<a>` 사용, React purity/immutability 위반, JSX 따옴표 오류
- 다수의 `<img>` 사용에 따른 LCP/대역폭 경고
- 불필요한 eslint disable, unused 변수
- 외부 Google Fonts/jsDelivr 의존으로 오프라인·CSP·개인정보 정책 검토 필요

린트의 `PopupLayer` cookie 변경 오류는 사용자 클릭 핸들러 안의 동작을 규칙이 오인했을
가능성도 있지만, 현재 기준으로 lint gate가 실패하는 사실은 변하지 않는다.

## 5. 화면/자산 감사

긍정적인 부분:

- `style.css`, `mobile_style.css`는 원본과 SHA-256이 완전히 동일하다.
- PC/mobile navigation, 홈 배너, 상품 그리드/상세, 카트/주문, 마이페이지, 게시판,
  관리자/입점사 화면이 별도 컴포넌트와 route로 폭넓게 존재한다.
- 대표 공개 화면은 개발 서버에서 HTML 200 응답을 반환했다.
- 상품/약관/CMS의 rich HTML은 주요 write path에서 `sanitize-html`을 거친다.

제한 및 차이:

- 원본 스킨의 HTML 템플릿과 JS를 그대로 쓰지 않고 React 마크업으로 재작성했기 때문에
  CSS 동일성만으로 픽셀 동일성을 보장할 수 없다.
- 원본 스킨 196개 중 Next public skin에는 CSS와 일부 이미지 18개만 있다. HTML/JS를
  React로 대체한 것은 정상이나, CSS가 참조하는 누락 이미지가 없는지는 화면별 검증이
  필요하다.
- 팝업 slider와 최근 본 상품 기록/화면·플로팅 drawer는 완료했으나 일부 외형 UX는 단순화되었다.
- 현재 Chrome 연결을 통해 storefront 홈과 관리자 로그인 렌더링은 확인했다. 390px
  뷰포트의 고정 폭 가로 넘침은 반응형 UI 개편 대상이며 migration 기능 결함으로
  분류하지 않는다.

권고: 원본 PHP와 Next.js를 동일 시드 데이터로 동시에 띄운 뒤 최소 1440px PC,
390px mobile에서 홈/목록/상세/카트/주문/회원/게시판 15~20개 핵심 화면의 screenshot
diff와 접근성 tree 비교를 CI 산출물로 남긴다.

## 6. 최초 기능별 판정(수정 전 상태)

| 기능군 | 판정 | 비고 |
|---|---|---|
| 홈/배너/카테고리/상품목록 | 부분 완료 | 주요 기능 있음, 일부 UX·자산 차이 |
| 검색/베스트/신상품/기획전 | 부분 완료 | 기본 흐름 있음, 검색 UX 단순화 |
| 상품상세/옵션/찜 | 부분 완료 | 리뷰·조회수·최근상품 없음 |
| 회원/로그인/가입 | 대체로 완료 | 소셜은 실키 미검증, 휴면 복구 없음 |
| 장바구니/주문 | 대체로 완료 | 테스트 존재, 일부 레거시 정책 단순화 |
| 결제/알림 | 결제 완료·알림 부분 완료 | R/V·현금영수증과 선정 후 PG 어댑터 절차 완료; 외부 알림 운영환경 검증 남음 |
| 게시판/CMS | 부분 완료 | 4종만, vendor board 및 고객 수정/삭제 없음 |
| 관리자 | 기능 폭 넓음 / 보안 실패 | 액션 인증 누락이 배포 차단 요소 |
| 입점사 | 기능 폭 넓음 / 추가 검증 필요 | 역할 검사 있는 액션과 없는 공용 helper 구분 필요 |
| 통계/로그 | 부분 완료 | 운영 핵심 일부 구현, 원본 집계 차원 축소 |
| cron/배송추적 | 구조 완료 | 외부 scheduler와 실 API key 필요 |
| SQL/Prisma | 신규 설치 검증 완료 | fresh install 29개 migration과 seed 성공 |
| SEO/feed | 부분 완료 | sitemap/robots 정상, feed 설정 off 시 404 |

## 7. 권고 조치 순서

### P0 — 배포 전 필수

1. 모든 exported backoffice mutation에 admin/vendor 인증과 소유권 검사를 추가한다.
2. 두 앱 build 오류와 lint error를 0으로 만든다.
3. 빈 DB에서 재현 가능한 migration/seed 명령과 CI smoke test를 만든다.
4. `AUTH_SECRET`, `CRON_SECRET`, PG callback 검증, cookie secure 옵션을 운영 환경 기준으로
   재점검한다.

### P1 — 정상 운영 전 필수

1. 업로드를 공유 object storage로 이전한다.
2. 결제사 선정 후 `PAYMENT_ADAPTER_GUIDE.md`에 따라 PG 성공/실패/중복 callback→취소/
   부분환불→재고·쿠폰·마일리지 복원 E2E를 실제 MySQL에서 자동화한다.
3. PHP 대비 기능 패리티 표에서 차이가 없음을 유지 검증한다. 리뷰/상품문의 답변/
   최근상품/배송 Excel 등은 현재 구현·검증 완료 항목이다.
4. 신규 설치 후 핵심 주문·쿠폰·마일리지·교환반품 시나리오의 DB 상태를 통합 테스트한다.

### P2 — 품질 및 인수인계

1. PC/mobile visual regression을 구축한다.
2. stale `MIGRATION.md`를 현재 상태 문서와 역사적 결정 로그로 분리한다.
3. 루트에 `lint`, `test`, `typecheck`, `build`, `db:migrate`, `verify` 스크립트를 제공한다.
4. 오류 관측, 백업/복구, cron 실패 알림, 결제 reconciliation 절차를 문서화한다.

## 8. 최종 인수 기준

다음 조건을 모두 만족해야 “정상 마이그레이션 완료”로 재판정할 수 있다.

- 두 앱 lint/test/build 모두 성공
- 무인 fresh install 성공
- 관리자/입점사 권한 우회 테스트 통과
- 핵심 주문/Mock 결제/환불 E2E 통과; 실제 PG E2E는 결제사 선정 후 운영 활성화 조건으로 분리
- 승인된 기능 패리티 표에서 미합의 gap 없음
- PHP 기능 대비 Next.js 기능 차이 없음
- 빈 DB migration·seed 및 핵심 SQL 통합 테스트 통과
- 배포 환경에서 이미지 영속화, cron, SMTP/SMS 실연동 확인

결제는 사용자 기준에 따라 완료로 재분류했고, 확인된 PHP 구현 기능의 기존 스코프아웃
항목도 후속 구현으로 닫았다. 빌드·권한·fresh install·Core 회귀·HTTP smoke는 통과했다.
현재 최종 판정은 **기능 기준 통과**이다. 반응형 UI 개편에 따른 폭·스타일 차이는
별도 디자인 작업으로 관리하며 migration 통과 여부를 변경하지 않는다.
외부 서비스 운영키와 object storage는 신규 쇼핑몰 배포 구성 단계의 조건으로 분리한다.
